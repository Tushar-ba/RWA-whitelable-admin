export enum TransactionType {
  BUY = "buy",
  SELL = "sell",
}

export enum TransactionStatus {
  PENDING = "pending",
  COMPLETED = "completed",
  FAILED = "failed",
}

export enum MetalType {
  GOLD = "gold",
  SILVER = "silver",
}

export enum AccountStatus {
  UNVERIFIED = "unverified",
  VERIFIED = "verified",
  ACTIVE = "active",
  SUSPENDED = "suspended",
}

export enum WalletLabel {
  PRIMARY = "primary",
  SECONDARY = "secondary",
}

export enum GiftingStatus {
  SUCCESS = "success",
  FAILED = "failed",
  PENDING = "pending",
}

export enum RedemptionStatus {
  PENDING = "pending",
  APPROVED = "approved",
  COMPLETED = "completed",
  FAILED = "failed",
}

export enum TokenType {
  GOLD = "GOLD",
  SILVER = "SILVER",
}

export enum NetworkType {
  ETHEREUM = "Ethereum",
  SOLANA = "Solana",
  PRIVATE = "Private",
}
